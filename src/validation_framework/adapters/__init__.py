"""AG_EGSVF_V1 adapters -- read-only translators from native AG evidence into
StrategyValidationRecord. Adapters never rerun a strategy engine, never re-resolve
outcomes, never recompute a campaign counter, and never mutate the evidence they cite.
See each adapter module's docstring for the exact evidence sources used.
"""
