# ST_M15_SESSION_SWEEP_RESEARCH_V1 — Lineage Manifest

```
strategy_id            = ST_M15_SESSION_SWEEP_RESEARCH_V1
version                = 0.1.0
lineage_type            = SOURCE_INSPIRED_RESEARCH
status                  = RESEARCH_ONLY
economic_status         = NOT_EVALUATED
source_replication_claim   = false
source_semantic_parity_claim = false
economic_edge_claim     = false
demo_authorized         = false
live_authorized         = false
```

**Provenance parents (neither controls the strategy after this baseline freeze):**
1. `ST_SESSION_TRADING_SOURCE_V1 v0.1.0` — AG's own frozen source-reconstruction candidate (`FROZEN_LOW_CORRESPONDENCE_INTERPRETATION`). Detection+Management core forked (`FORK_COPY_FROZEN`), not imported.
2. `D:\ddev\Session Trade Codex` — external, independently-developed project (HEAD `d026f09a6fc79b5f0eabfd438252d0a545cb4d05` as observed during ES-R0A). Corroborating/provenance evidence only — see `D_DRIVE_PROVENANCE_MANIFEST.md`. **Not a runtime dependency; no code imported from it.**

**Established by:** ES-R0, following ES-S6 (`PATH_B_RESEARCH_FORK`) and ES-R0A (`OPTION_C_CONTROLLED_FORK_FROM_D_DRIVE`).

This strategy makes no claim to replicate the original presenter's actual trade-eligibility rules (ES-S6: `SOURCE_ELIGIBILITY_COMPLETENESS = INSUFFICIENT`). It generates the full, unfiltered deterministic Sweep-occurrence population as an independent research object.
