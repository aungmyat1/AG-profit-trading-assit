# ES-R1 Candidate Budget

| Hypothesis | Candidates (incl. baseline) |
|---|---|
| R-H01 | 4 (`H01_C0..C3`) |
| R-H02 | 5 (`H02_C0..C4`) |
| R-H03 | 5 (`H03_C0..C4`) |
| **Total first-pass evaluations** | **14** |

Baseline (`*_C0`, the unfiltered `ALL_STRUCTURAL_OCCURRENCES` population) is identical across all three hypotheses and is computed/cached once, reused for all three comparisons — not treated as 3 separate economic runs.

No candidate may be added beyond this list within this experiment. Widening this budget requires a new preregistration.
