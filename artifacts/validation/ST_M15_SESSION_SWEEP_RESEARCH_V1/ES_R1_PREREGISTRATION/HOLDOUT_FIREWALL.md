# ES-R1 Holdout Firewall

```
ES-R1  HOLDOUT_ACCESS_COUNT = 0
ES-R2  HOLDOUT_ACCESS_COUNT = 0
ES-R3  HOLDOUT_ACCESS_COUNT = 0
ES-R4  HOLDOUT_ACCESS_COUNT = 0
```

Final holdout remains `SEALED` through every stage above. Only after all candidate/search decisions are frozen (post ES-R4) may ES-R5 receive explicit, separate owner authorization for **one-shot** holdout access. No holdout dataset has been identified, selected, or named in this or any prior mission in this lineage.

## MT5 identity firewall (carried forward, P15)

`MT5_MAGIC_NUMBER_COLLISION_RISK = OPEN` (unchanged from ES-R0). No magic-number change in ES-R1. No Demo integration. No MT5 order execution. Before any eventual Forward Demo stage, a unique `PROJECT × STRATEGY × ENVIRONMENT` execution identity must be assigned and verified — not assumed resolved by this record.
