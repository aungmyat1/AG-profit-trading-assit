# SWEEP_SOURCE_SEMANTIC_MATRIX

Component: `SWEEP` of `SESSION_TRADING_STRATEGY_SOURCE_FAMILY`
Candidate implementation audited: `ST_ASIAN_SWEEP_5R_V1 v1.1.1` — **executable code**, not just its YAML declaration (S0 only read the YAML; this mission re-audits against the live signal/proposal path).

| # | Rule | Classification | Repository provenance | Note |
|---|---|---|---|---|
| 1 | Asian session 00:00–07:00 UTC | PARTIAL_MATCH | `strategies/ST_ASIAN_SWEEP_5R_V1.yaml:47-48` | Implemented reference window is `00:00`–**`06:00`**, not `07:00`. YAML's own comment (lines 36-38) already flags this as a deliberate deviation from `config/canonical_sessions.yaml`'s `asian` (00:00-06:00, which actually matches) vs. the *source's* stated 00:00-07:00. |
| 2 | Asian high/low construction | EXACT_MATCH | `src/strategy_engine/session/setups.py` `ReferenceBox` (session_high/session_low over the reference window) | Highest high / lowest low over the reference window, as source describes. |
| 3 | Range freeze | EXACT_MATCH | Same `ReferenceBox`, passed unchanged as `box.session_high`/`box.session_low` into `entry_2_sweep` | Computed once, held fixed for the trade session. |
| 4 | Long sweep | EXACT_MATCH | `setups.py:132-141` (`lower_swept`, `Direction.LONG`) | Wick below `session_low`, close back above it. |
| 5 | Short sweep | EXACT_MATCH | `setups.py:122-131` (`upper_swept`, `Direction.SHORT`) | Wick above `session_high`, close back below it. |
| 6 | Wick penetration requirement | EXACT_MATCH | `setups.py:110-111`: `candle.high > box.session_high` / `candle.low < box.session_low` | Strict penetration (config `sweep_classifier: penetration STRICT`). |
| 7 | Close-inside requirement | EXACT_MATCH | `setups.py:110-111`: `candle.close < box.session_high` / `candle.close > box.session_low` | This resolves S0's rule #7/#8 ambiguity toward the "close confirms sweep" source variant, not the "boundary-limit, no close needed" variant — see P3. |
| 8 | Directional symmetry | EXACT_MATCH | `setups.py:122-141` | Long/short branches are structurally mirror-symmetric. |
| 9 | Entry trigger | EXACT_MATCH (against ONE of three contradictory source variants — see P3) | `setups.py:110-141` | First qualified candle chronologically in the post-session window; dual-side same-candle sweep -> `AMBIGUOUS`/no trade (`setups.py:113-120`), matching the source benchmark's own Setup #10-style filtered/neutral outcome pattern (existence only, not the same condition). |
| 10 | Entry price | PARTIAL_MATCH | `setups.py:128` (`entry_reference=max(candle.open, candle.close)`), `setups.py:138` (`min(candle.open, candle.close)`) | This is the sweep candle's body edge nearer the sweep side — equal to `close` only when the candle's open lands on the far side, otherwise equal to `open`. Not literally always "close," though the YAML comment calls it `Sweep_Candle_Body_Close`. |
| 11 | **SL calculation** | **CONTRACT_CONFLICT** | YAML: `strategies/ST_ASIAN_SWEEP_5R_V1.yaml:108-109` (`stop_loss_range_pct: 0.25`) vs. live code: `setups.py:129,139` (`stop_reference=candle.high` / `candle.low`, i.e. the sweep wick extreme) | **`src/strategy_engine/session/candidate_stop_models.py`'s own module docstring states this explicitly**: *"the strategy's already-declared, already-parsed, but never consumed contract... loaded into `StrategyConfig.risk.stop_loss_range_pct`... but never read by any calculation site — confirmed by repo-wide grep, 2026-09-06"* and *"v1.1.1's frozen stop_reference = sweep-candle wick behavior is preserved exactly as-is."* The live SL is the raw sweep-wick distance, **not** 25% of the Asian range. A dormant `RESEARCH_CANDIDATE` module (`candidate_stop_models.py`, `authority=NONE`, version `1.1.2-RC1`) implements the declared 25%-of-range formula but is **not wired into the live path**. |
| 12 | 25%-Asian-range geometry | **CONTRACT_CONFLICT** (same finding as #11) | same as #11 | The external source's core universal SL rule (S0 rule #10, `SOURCE_VERIFIED`) is the declared-but-dormant model, not the live one. |
| 13 | 5R calculation | EXACT_MATCH (as a multiplier only — see #11/#12 caveat) | `src/post_asian_pilot/proposal.py:102-107` (`total_target_r = strategy.total_target_r`; `tp2 = entry ± total_target_r * risk_distance`) | Live and wired. `total_target_r=5.0` is read and applied. **Caveat**: `risk_distance` here is `abs(entry - stop_loss)` (`proposal.py:96,103`), which inherits the wick-based stop from #11/#12 — so "5R" is honored as a multiplier, but the R-unit itself is not the source's 25%-of-range unit. |
| 14 | 75% partial | EXACT_MATCH | `strategies/ST_ASIAN_SWEEP_5R_V1.yaml:116-117` (`volume_pct: 0.75`), consumed via `src/execution/validator.py:76-90` (`leg1_take_profit`) | |
| 15 | Opposite-boundary partial target | EXACT_MATCH | `validator.py:84-89`: `if leg1.target_type != "OPPOSITE_SESSION_BOUNDARY": return None` then `signal.box_high`/`box_low` | Live and wired, directly matches source Sweep management rule. |
| 16 | BE transition on partial fill | PARTIAL_MATCH — not independently confirmed live-executed | `strategies/ST_ASIAN_SWEEP_5R_V1.yaml:119` (`action_on_fill: MOVE_RUNNER_SL_TO_BREAKEVEN`) declared; no live state-machine code performing an actual mid-trade SL modification was located in this pass (only the owner-signed *offline outcome resolver*, `scripts/resolve_forward_shadow_outcomes.py`, appears to model BE transition for *resolution* purposes, not as a live order-modification action). | Flagged, not asserted either way — a deeper audit of live order-management code (not attempted this pass, to respect scope) would be needed to fully confirm. |
| 17 | Remaining 25% runner to 5R | EXACT_MATCH | `proposal.py:102-108` (`tp2`) | See #13 caveat on the R-unit. |
| 18 | Range/no-trade filter | **IMPLEMENTATION_MISSING** | `strategies/ST_ASIAN_SWEEP_5R_V1.yaml:74-79` declares `range_session_check`/`max_range_pips_eurusd: 25.0`; grep confirms this field is consumed **only** by an unrelated module, `src/session_tribranch_research/replay.py:103,151` (a different, separate research strategy) — **not** by `ST_ASIAN_SWEEP_5R_V1`'s own live path (`entry_2_sweep`, `post_asian_pilot/decision.py`) | The source's own confirmed too-tight-range no-trade condition (S0 rule #17) has no live-enforced counterpart in this strategy at all. |
| 19 | Session entry window | SOURCE_AMBIGUOUS (see P4) | `strategies/ST_ASIAN_SWEEP_5R_V1.yaml:50-53,61-64`: `07:00-11:00` (ASIAN_LONDON) / `12:00-15:00` (LONDON_NEWYORK) | Matches none of the source chat's three contradictory windows (07:00-16:00 / 07:00-18:00 / 07:00-22:00) — see P4, not adjudicated here. |
| 20 | Session expiry / max hold | IMPLEMENTATION_EXTRA | `strategies/ST_ASIAN_SWEEP_5R_V1.yaml:129` (`time_invalidation: "15:00 GMT"`); `src/post_asian_pilot/proposal.py:33` (`PROPOSAL_EXPIRY_MINUTES = 15`, one M15 bar) | Real, deliberate repo policy with its own rationale, not attributable to (or contradicted by) the source material, which never specified proposal-level expiry (S0 rule #9, `MISSING`). |
| 21 | Maximum entries/session | PARTIAL_MATCH — provenance not fully confirmed this pass | `strategies/ST_ASIAN_SWEEP_5R_V1.yaml:54,65` (`max_entries_per_session: 1`); likely enforced via `DailyTradeSlot.try_claim()` referenced in `proposal.py:65-69`'s comment, not independently verified in this pass | Consistent with source's implicit one-trade-per-clean-setup framing in the *original* flowchart (before the chat log's own later multi-trade curve-fitting), but the enforcement mechanism itself wasn't traced end-to-end here. |
| 22 | M15 intrabar SL/TP ordering | EXPLICITLY_UNRESOLVED — see `SWEEP_M15_AMBIGUITY_AUDIT.md` | `scripts/resolve_forward_shadow_outcomes.py:67,319,326,371,378` (`AMBIGUOUS_SEQUENCE`) | Never assumes intrabar order; directly **contradicts** the source chat's own "conservative stop-first" assumption (S0 rule #18) — this repo's convention is stricter/safer than the source. |
| 23 | Friction | PARTIAL_MATCH | `strategies/ST_ASIAN_SWEEP_5R_V1.yaml:110-111` (`max_spread_allowed_pips: 2.0`, `slippage_limit_points: 10`); `src/fx_friction_research/cost_model.py`, `src/performance/cost_model.py` | These are the repo's own signed friction constants for this strategy's *offline outcome resolution*, not derived from the source's own (internally inconsistent) "-21R" narrative haircut (S0 rule #19, `AMBIGUOUS`). No relationship to the source's friction claim was found or assumed. |

## Summary counts
- EXACT_MATCH: 11
- PARTIAL_MATCH: 5
- SOURCE_AMBIGUOUS: 1
- IMPLEMENTATION_EXTRA: 1
- IMPLEMENTATION_MISSING: 1
- **CONTRACT_CONFLICT: 2** (SL calculation, 25%-Asian-range geometry — same underlying finding)
