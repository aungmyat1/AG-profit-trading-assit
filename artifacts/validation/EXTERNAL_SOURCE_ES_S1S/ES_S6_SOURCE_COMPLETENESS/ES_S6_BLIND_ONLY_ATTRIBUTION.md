# ES-S6 Blind-Only Occurrence Attribution (P7)

60 of the 66 frozen ES-S3 occurrences have no reference-table counterpart (ES-S4). Grouped attribution, not per-occurrence invention of a reason:

| Group | Count | Classification |
|---|---|---|
| Occurrences on days/times whose reference correspondence is undetermined because the reference clock itself is `UNRESOLVED` (ES-S5A) | up to all 60 in part | `COMPARISON_CLOCK_UNRESOLVED` — every mapping decision in ES-S4/ES-S5A depends on the disputed clock interpretation; a blind-only classification under M0 is not guaranteed to remain blind-only under a different, still-unverified clock reading |
| 8 occurrences already flagged `SOURCE_RULE_DEPENDENT_UNRESOLVED` (entry-cutoff or tight-range uncertainty) | 8 | `UNRESOLVED_SOURCE_RULE` — these occurrences' own eligibility is already known to be undetermined independent of the reference table |
| 52 occurrences classified `SOURCE_DETERMINISTIC` with no reference match | 52 | `REFERENCE_SELECTION_UNKNOWN` combined with `QUALITATIVE_SOURCE_FILTER_POSSIBLE` — the presenter's own described discretionary process (ES-S6 authority matrix, row 20) plausibly explains why a structurally valid mechanical sweep would not appear in an 18-event curated table, but no specific rule can be pointed to for any individual occurrence |
| Any occurrence for which a specific numeric filter (range threshold, wick ratio, max-entries) can be shown to explain its exclusion | 0 confirmed | `NO_SUPPORTED_EXCLUSION_RULE` — no such threshold is source-authoritative (ES-S6 eligibility inventory), so none can be cited as *the* reason for any specific occurrence's absence |
| Any occurrence provably excluded by a rule with direct source backing | 0 | `KNOWN_SOURCE_FILTER` — not used for any occurrence; no rule reaches this bar |

**No manufactured reason was assigned merely because the reference table lacks a matching row.** The honest attribution is a mix of unresolved clock uncertainty (upstream of everything), a small number of occurrences whose own source-rule status is already known to be undetermined, and a large residual (52) for which only a *qualitative*, non-quantifiable discretionary-filtering explanation has any source support at all — and that support does not extend to explaining any *specific* occurrence's exclusion.
