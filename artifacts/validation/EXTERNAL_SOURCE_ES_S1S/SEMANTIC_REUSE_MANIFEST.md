# SEMANTIC_REUSE_MANIFEST — ST_SESSION_TRADING_SOURCE_V1 / SWEEP implementation

| Primitive | Classification | Detail |
|---|---|---|
| `Candle` (OHLC dataclass) | **REUSE_UNCHANGED** | Imported verbatim from `strategy_engine.session.candles.Candle`. Pure, generic, strategy-neutral data structure with zero behavior attached — no semantic risk. |
| Asian range construction (highest-high / lowest-low over a fixed window) | **NEW_SOURCE_IMPLEMENTATION** | Written fresh in `session_trading_source_v1/asian_range.py`, using this candidate's own 00:00–07:00 UTC window — deliberately NOT importing `strategy_engine.session.setups.ReferenceBox`, whose default window (00:00–06:00, per `ST_ASIAN_SWEEP_5R_V1.yaml`) does not match this candidate's frozen source contract. |
| Strict sweep penetration predicate | **NEW_SOURCE_IMPLEMENTATION** (semantics independently re-derived to match a proven `EXACT_MATCH`) | `session_trading_source_v1/sweep.py` re-derives the same predicate ES-S1S proved `EXACT_MATCH` against `entry_2_sweep` (`setups.py:110-111`), but does not import that function, because it entangles the predicate with `stop_reference`/`risk_distance` fields (the wick-based geometry) this candidate must not reuse. |
| Close-back-inside predicate | **NEW_SOURCE_IMPLEMENTATION** (same reasoning as above) | Same file, same entanglement-avoidance rationale. |
| Opposite-boundary calculation | **NEW_SOURCE_IMPLEMENTATION** | `session_trading_source_v1/stop_target.py::opposite_boundary_target` — trivial and unambiguous, but written fresh rather than imported from `execution/validator.py::leg1_take_profit`, which is coupled to `ST_ASIAN_SWEEP_5R_V1`'s own `StrategyConfig`/`TradeSignal` types. |
| Wick-based stop geometry | **NOT_REUSED_SEMANTIC_CONFLICT** | `entry_2_sweep`'s `stop_reference=candle.high/low` and `post_asian_pilot/proposal.py`'s wick-derived `risk_distance` are explicitly NOT reused anywhere in this candidate — this is the one primitive the mission named outright as forbidden, and the one point of genuine semantic conflict between the two lineages (ES-S1R P5/P6). |
| Existing session-window cutoffs (07:00–11:00 / 12:00–15:00, `ST_ASIAN_SWEEP_5R_V1.yaml`) | **NOT_REUSED_SEMANTIC_CONFLICT** | Not inherited merely because they already exist, per this mission's explicit instruction — this candidate uses its own (partially unresolved) window facts from ES-S1R P7 instead (07:00 start verified, cutoff `SOURCE_AMBIGUOUS`, 22:00 management-end). |
| Same-bar/intrabar ambiguity philosophy (never assume STOP_FIRST/TARGET_FIRST) | **NEW_SOURCE_IMPLEMENTATION**, independently justified | `session_trading_source_v1/partial_be.py` reaches the same `INTRABAR_ORDER_UNRESOLVED` posture as `scripts/resolve_forward_shadow_outcomes.py`'s `AMBIGUOUS_SEQUENCE` convention, but the code itself is not imported/shared — the *policy* (never assume order) is independently re-derived from first principles for this candidate's own geometry, not copied. |

No other repository code is imported, called, or otherwise reused by `session_trading_source_v1`.
