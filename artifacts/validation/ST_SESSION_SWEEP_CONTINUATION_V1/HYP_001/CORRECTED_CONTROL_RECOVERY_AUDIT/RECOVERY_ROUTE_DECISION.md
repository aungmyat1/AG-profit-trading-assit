# Recovery-Route Decision (P5/P9)

- **Route A (existing provenance-compatible richer artifact): unavailable.** Confirmed by direct inspection of every population artifact reachable from `main` and from the `refactor/architecture-boundary-hardening-v3` branch — none retains `reference_high`/`reference_low` or post-entry candle evidence (`EXISTING_ARTIFACT_INVENTORY.md`).
- **Route B (fresh deterministic occurrence reconstruction from frozen raw inputs): available for all three populations**, with one open sub-item (`CONTRACT_AMBIGUOUS`: H1-bias-gate vintage question for GEN_001/GEN_002A) to be resolved explicitly in that future mission, not here. All required raw dataset legs for GEN_002 are hash-verified in-repo; GEN_001's three legs and GEN_002A's M1 leg are hash-verified on `D:\`; GEN_002A's H1/M15 legs need one more confirmatory lookup before that population specifically is ready (not a blocker for GEN_001/GEN_002, and not attempted in this mission per its own read-only scope).
- **Route C (blocked): not selected** — Route B is demonstrably viable.

## Decision

```
FRESH_REPLAY_PREREGISTRATION_REQUIRED
```

This is the single, exclusive decision returned by this mission. No replay is executed here. A new, additive preregistration amendment (per `ROUTE_B_CONTRACT.md`) is required before any execution.
