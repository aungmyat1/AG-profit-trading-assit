# Owner-approved historical analytical metadata for ONE specific dataset file.
#
# Scope (binding, per owner authorization 2026-09-02): HISTORICAL_ANALYSIS_ONLY. This
# tick_size may be used for historical market-structure/equal-level-tolerance analysis
# during research replay (market_structure.tiers.analyze_structure_tiers and anything
# that calls it, e.g. historical_replay/stage2.py's M1 inducement detection and
# src/large_smc_research's C11 target adapter) -- see
# historical_replay/symbol_metadata_manifest.py, which loads and validates this file.
#
# It is NOT live MT5 metadata, NOT a current broker specification, and must NEVER be
# used for order execution, stop-loss placement, risk sizing, tick value, contract
# size, or volume limits. Any SymbolMeta constructed from this manifest is tagged
# metadata_source=SYNTHETIC_RESEARCH (mt5.symbol_resolver.METADATA_SOURCE_SYNTHETIC_
# RESEARCH), the same tag crypto_symbols.py's own synthetic metadata already uses --
# execution.adapter.require_exchange_verified_metadata() already refuses anything
# tagged this way, so this can never silently reach an execution path.
#
# This authorization is bound to ONE dataset file, verified by dataset_fingerprint --
# it is NOT a general EURUSD rule and does not apply to any other historical dataset.

dataset_id: EURUSD_M5_202504211715_202607310000
symbol: EURUSD
tick_size: 0.00001
metadata_scope: HISTORICAL_ANALYSIS_ONLY
authority: OWNER_APPROVED_DATASET_MANIFEST
approval_date: "2026-09-02"
# sha256 of the exact source file, computed 2026-09-02:
#   python -c "import hashlib; h=hashlib.sha256(); f=open(r'D:\EURUSD_M5_202504211715_202607310000.csv','rb'); [h.update(c) for c in iter(lambda: f.read(1024*1024), b'')]; print('sha256:'+h.hexdigest())"
dataset_fingerprint: "sha256:0c269b9aae1a610cddc87b61f30c12bcd89bcd25c7276e5406a6e36c409957e8"
source_file: "D:\\EURUSD_M5_202504211715_202607310000.csv"
