# Optional Web boundary shell

Reserved for an optional UI. It may consume authorized read models and record an
authenticated owner decision through the control boundary. It must not import broker or
execution mutation code.
