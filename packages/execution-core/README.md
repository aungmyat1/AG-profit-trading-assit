# Execution Core boundary shell

Reserved for future broker-independent execution decisions/contracts. It must not import
MT5 or contain a broker mutation adapter. Existing execution code remains authoritative.
