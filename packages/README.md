# Packages

New architecture packages live here while the existing `src/` runtime remains in place.
Contracts are the sole frozen P1 package; other directories are dependency boundary
shells only until their later migration gates.
