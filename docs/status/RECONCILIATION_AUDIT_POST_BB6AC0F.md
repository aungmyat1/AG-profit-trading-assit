# Reconciliation Audit — Post bb6ac0f

Status: HISTORICAL_STATUS (dated evidence snapshot). Generated by AG_PROJECT_LIVE_CONTROL_PLANE_V1 Sprint 1, 2026-09-17.
Do not use this document as the live source of truth after `docs/status/PROJECT_LIVE_STATUS.md` exists — that generated file supersedes this one for "where are we now." This document records the one-time reconciliation that unblocked it.

## 1. Repository Identity

| Field | Value |
|---|---|
| Branch | `main` |
| HEAD | `bb6ac0f` (docs(ssc): reseal v1.0.1 rollover manifest against current ancestry) |
| origin/main | 1 commit behind local HEAD (local ahead 1, not yet pushed) |
| Working tree | Clean except one untracked foreign path (see §2) |

## 2. Concurrency / Foreign-WIP Inventory

| Path | Classification | State |
|---|---|---|
| `artifacts/validation/EXTERNAL_SOURCE_ES_S1S/ES_S4_COMPARISON/` (9 files) | FOREIGN_CONCURRENT_WIP | Untracked, uncommitted, mtimes 2026-09-17 11:13-11:14 (recent, likely concurrent session) |

`ES_S4_COMPARISON` claims to compare the 66 frozen ES-S3 blind occurrences against an 18-row external reference table, reporting a low match rate (0 exact / 2 probable / 4 ambiguous / 12 reference-only / 60 blind-only) and three unresolved hypotheses (UTC/session-boundary offset, entry-price semantic ambiguity, discretionary-vs-mechanical filtering). It explicitly states no source rule was changed in response.

**Status: `ES_S4 = FOREIGN_UNCOMMITTED_WIP`.** Its contents are descriptive claims only, not independently verified, and carry no governance weight until committed through the project's normal freeze process. It is preserved byte-for-byte and was not read for any purpose other than describing what it claims to contain.

No other pre-existing or concurrent dirty/untracked paths were found. No `MISSION_OWNED` or `UNKNOWN` paths existed prior to this mission's own writes.

## 3. Ancestry Verification

```
02d7b29  fix(ssc): remediate v1.0.1 partial-target direction
   |
f805a36  test(lsmc): scope lifecycle freeze to Large SMC
   |
24f3032  test(external): freeze ES-S3 blind source replay
   |
873dd68  feat(session-trading-source): add ST_SESSION_TRADING_SOURCE_V1 candidate and SSC v1.0.1 rollover  [MIXED SCOPE]
   |
bb6ac0f  docs(ssc): reseal v1.0.1 rollover manifest against current ancestry   <- HEAD
```

Verified directly via `git log`/`git show` (not assumed). `actual_lineage_status = MATCHES_EXPECTED`.

## 4. 873dd68 Mixed-Commit Classification

- **COMMIT_SCOPE = MIXED**, confirmed. Categories present: SSC_VERSION_ROLLOVER, EXTERNAL_SOURCE, LARGE_SMC, BTC, PROPOSAL_LEDGER, VALIDATION_EVIDENCE (cross-cutting), DEVELOPER_CONFIG. The commit message itself discloses the mixing.
- **CONTENT_INTEGRITY = PASS.** Every category's diff content matches its label on spot-check:
  - SSC files changed only `1.0.0` → `1.0.1` version literals (or replaced a hardcoded literal with an import of the same constant); `DEMO_AUTHORIZED`/`DEMO_ELIGIBLE` untouched (both `False`).
  - `state/proposal_ledger/proposal_ledger.json`: 37→56 keys, **zero existing keys modified or removed**; all 19 new entries carry `execution_authority: "NONE"` / `status: "NOT_INCLUDED"` — purely additive, no authority granted.
  - New `V1_0_1_VERSION_ROLLOVER_MANIFEST.json` is internally consistent, explicitly enumerates what it did and did not touch, and records `demo_authority_changed: false`, `live_authority_changed: false`.
  - `.vscode/settings.json` / `pyrightconfig.json` changes are cosmetic tooling config only.
- **PROVENANCE_QUALITY = MIXED_BUT_RECONCILED.** The rollover manifest and the follow-up commit `bb6ac0f` explain and diff-verify that 873dd68's SSC-relevant content is byte-identical to the originally-audited rollover diff — nothing was corrupted by the bundling.
- **`history_rewrite_required = false`.** No history rewrite is needed or recommended.

### bb6ac0f Reseal Verification

Touches exactly one file (`V1_0_1_VERSION_ROLLOVER_MANIFEST.json`, +11/-5). Adds `lsmc_freeze_scope_prerequisite_commit: f805a36` and `pre_rollover_head_at_resume: 24f3032`, refreshes regression-test counts, and states the SSC-relevant content of 873dd68 was diff-verified byte-identical to what was originally audited. No source or config file changed. Legitimate manifest reseal, not a semantic or authority change.

## 5. Strategy Version / Authority Matrix

| Strategy | Version | Lifecycle Stage | demo_authorized | live_authorized | Execution Adapter |
|---|---|---|---|---|---|
| ST_ASIAN_SWEEP_5R_V1 | — | — | false | false | — |
| SESSION_TRADE_V1 | — | — | true (ASIAN_LONDON only; LONDON_NEWYORK=false) | false | — |
| SMC_3R_V1 | — | — | false | false | — |
| ST_SESSION_SWEEP_CONTINUATION_V1 (SSC) | 1.0.1 (5 authorities coherent) | OFFLINE_RESEARCH | false | false | — |
| ST_LARGE_SMC_V1 | 1.0.7 | RESEARCH_DRAFT | false | false | NOT_CONNECTED |
| ST_LIQUIDITY_SWEEP_RETEST_V1 (BTC/FX) | 2.0.0 | FORWARD_RESEARCH | false | false | — |
| ST_SESSION_TRADING_SOURCE_V1 (ES candidate) | 0.1.0 | Not registered / not admitted (research spec only) | false (no authority granted) | false | — |
| R8_OBM_V1 | — | — | false | false | — |

**No strategy has `live_authorized: true` anywhere in the repository.** Only `SESSION_TRADE_V1` (ASIAN_LONDON cycle) currently has `demo_authorized: true`.

**AUTHORITY_CHANGED_BY_873dd68 = false**, confirmed by diff: no file matching `demo_authorized`/`live_authorized` globs changed in 873dd68, and none of `strategies/registry.yaml`, `strategies/ST_LIQUIDITY_SWEEP_RETEST_V1.yaml`, `strategies/ST_LARGE_SMC_V1.yaml`, or `web/server.ts` appear in its changed-file list at all.

## 6. Validation-State Matrix

### SSC (ST_SESSION_SWEEP_CONTINUATION_V1)

| Fact | State |
|---|---|
| SSC_CODE_IDENTITY | 1.0.1, coherent across lifecycle registry, package constant, adapter, decision-replay default, SVOS export |
| SSC_SEMANTIC_REMEDIATION | VERIFIED |
| SSC_VERSION_ROLLOVER | VERIFIED (resealed bb6ac0f) |
| Fail-closed guard vs stale 1.0.0 | VERIFIED (raises `LifecycleRegistryError`; test-covered) |
| SSC_CORRECTED_BASELINE_ECONOMICS | NOT_ESTABLISHED (`economic_effect_evaluated: false`, `historical_economic_replay_run: false`, `new_population_generated: false`) |
| SSC_DEMO_ELIGIBILITY | BLOCKED (`demo_eligible: false`, `demo_authorized: false`, `live_authorized: false`) |
| HYP_002 (setup selectivity) | Terminal — `FALSIFIED_ON_GEN_002_EVIDENCE` / `VALIDATED_NEGATIVE`. Rerun/reinterpretation forbidden. |
| HYP_003 (mechanism-generation) | `NOT_JUSTIFIED` — closed without opening a new hypothesis lineage (a valid research stop, not a failure) |
| HYP_001 (exit-capture, v1.1.0 candidate) | Quarantined — `PENDING_BASELINE_REMEDIATION_REASSESSMENT`, `fresh_confirmation_runs: 0`, blocked at G1 (preregistration repair needed) |
| Next gate (repository-derived, not a literal string found in-repo) | Owner adjudication of HYP_001_EXIT_CAPTURE preregistration repair; no formalized "CORRECTED_V1_0_1_BASELINE_ECONOMIC_REPLAY" gate exists yet as a named artifact |

### External Source (ST_SESSION_TRADING_SOURCE_V1)

| Fact | State |
|---|---|
| Registration | Not in `strategies/registry.yaml` or lifecycle registry — research candidate only |
| ES_S3 | FROZEN, committed (`24f3032`); benchmark table never opened (`benchmark_reference_table_opened: false`) |
| ES_S4 | FOREIGN_UNCOMMITTED_WIP (see §2) — non-authoritative |
| Demo/Live authority | NONE (research spec only) |

### Large-SMC (ST_LARGE_SMC_V1)

| Fact | State |
|---|---|
| Version | 1.0.7, `RESEARCH_DRAFT` |
| f805a36 freeze scope | CONFIRMED narrow — protects only `schema_version` + the `ST_LARGE_SMC_V1` registry entry; other strategies' lifecycle entries are unaffected (test-proven by `tests/test_lsmc_frozen_core_scope.py`) |
| Friction campaign (WP3A.1) | **1 of the required ≥5 trading days collected** (2026-09-16, all 4 windows). No sessions yet for 2026-09-17. Do not infer completion from Task Scheduler configuration existing. |
| Friction policy contract | `PROPOSED`, owner signature required; `FRICTION_POLICY_SIGNABILITY: INSUFFICIENT_EVIDENCE` |
| Demo/Live authority | Both false; execution adapter NOT_CONNECTED |

### BTC (ST_LIQUIDITY_SWEEP_RETEST_V1, CRYPTO_PERP profile)

| Fact | State |
|---|---|
| Version | 2.0.0 (FOREX + CRYPTO_PERP profiles share this strategy) |
| Market-data authority | Bybit V5 linear-perpetual, public/unauthenticated |
| Observation campaign | **Blocked at 0/30** — every daily report 2026-09-11 through 2026-09-15 shows `decision: DATA_ERROR`, root cause `INSTRUMENTS_INFO_REQUEST_FAILED` (persistent Bybit 403 on `instruments-info`). No report generated since 2026-09-15; none yet for 09-16/09-17. |
| Qualified occurrences / evidence count | 0 / 0 |
| Demo/Live authority | Both false |
| Next safe action | Diagnose/restore Bybit market-data access; no strategy/contract change implicated |

## 7. Proposal / Execution Authority Reconciliation

- **Proposal ledger**: `state/proposal_ledger/proposal_ledger.json`, backed by `src/proposal_envelope/ledger.py::ProposalLedger` — durable, restart-safe (`JsonKeyValueStore`), 56 entries, all `FX:DECISION-*` (no BTC/Large-SMC proposals recorded yet).
- **Canonical proposal authority: still unreconciled.** Four proposal-identity schemes remain unmodified on HEAD: `src/proposals/identity.py`, `src/proposal_envelope/models.py::CanonicalProposal`, `src/ticket_delivery/identity.py`, `src/strategy_engine/sweep_retest/occurrence_identity.py`. This is a **pre-existing gap (WP7, not caused by this mission and not remediated by it)** — `src/api/app.py` still wires both the old `InMemoryProposalRegistry` (used for the actual `authorize-demo` execution gate) and the new durable `ProposalLedger` (read-only canonical-proposal endpoints) side by side rather than one superseding the other.
- **Execution gateway**: The sole path from a proposal to a real broker order is `POST /api/tickets/{approval_id}/authorize-demo` → `execution_service.authorize_demo_execution()`, which re-reads `demo_authorized` fresh from `strategies/registry.yaml` on every call (no caching) and fails closed if unregistered or false.
- **Second gateway retired**: `web/server.ts`'s real-mode `/api/execution/execute`, `/api/execution/claim`, `/api/execution/manage` branches are confirmed retired (`HTTP 410 EXECUTION_ROUTE_RETIRED`) under prior WP0A containment work, resolved 2026-09-11. Only the mock/simulated branch remains, with no broker contact. `/api/proposals/scan` (synthetic scanner) is quarantined via `marketDataSource: SYNTHETIC` / `executionEligible: false` tagging.
- **Conclusion**: No unauthorized execution path was found; no authority was changed by 873dd68 (see §5). The proposal-identity reconciliation (WP7) remains an open, pre-existing gap — noted for `docs/PROJECT_ROADMAP.md`'s owner-decision tracking, not fixed by this mission.

## 8. Documentation Drift

| Document | Classification |
|---|---|
| `docs/PROJECT_ROADMAP.md` | AUTHORITATIVE_CONTRACT — master plan, "one authority per layer," owns the live readiness matrix (R0-R9) conceptually |
| `docs/validation/AG_ACCELERATED_VALIDATION_PLAN_V1.md` | CURRENT_PLAN |
| `docs/validation/AG_AUTO_VALIDATION_ORCHESTRATOR_V1.md` | CURRENT_PLAN — only AVO-WP1 (read-only state model) is implemented; WP2-WP10 (DAG, ledger, scheduler, dispatcher, API) are unbuilt spec |
| `docs/validation/SSC_ONE_DAY_DEMO_PILOT_V2.md` | CURRENT_PLAN, self-declared `SPECIFICATION_ONLY — NOT ACTIVATED`, explicitly out-of-scope for AVO integration |
| `docs/status/*` (94 files) | HISTORICAL_STATUS (append-only dated snapshots per `LIVE_STATUS_MAINTENANCE.md`), except `STRATEGY_REGISTRY_STATUS.md` which self-flags `STALE_CURRENT_STATUS` |

No document under `docs/status/` currently functions as a continuously-current live-status generator. This is the gap `PROJECT_LIVE_STATUS.md` (Sprint 2) fills.

## 9. Existing AVO Architecture (for Sprint 2 reuse)

- `src/validation_orchestrator/status.py` implements `StrategyValidationStatus` (frozen dataclass) with a `semantic_tuple()` method that deliberately excludes `updated_at_utc` — the existing precedent for fingerprint/staleness comparison.
- `dataset_role_status` / `lineage_status` / `holdout_status` are deliberately hardcoded `NOT_TRACKED_BY_WP1` — a documented fail-closed gap, not to be filled by inference.
- Canonical fingerprint primitive: `src/post_asian_pilot/fingerprint.py::fingerprint()` (SHA-256 over `json.dumps(..., sort_keys=True, separators=(",",":"))`), already reused by `src/authorization/integrity.py`.
- Git-HEAD-stamped snapshot precedent: `scripts/generate_validation_ledger_snapshot.py` (`_git_head()`), separate from AVO-WP1.
- A parallel worktree (`research/ssc1d-ext-r1`) carries an identical copy of the WP1 files (inherited from main), not further advanced — no duplicate-effort conflict found.

## 10. Blockers

1. BTC observation campaign blocked at 0/30 by persistent Bybit `instruments-info` 403 (data-access issue, not a strategy defect).
2. Large-SMC friction campaign has only 1 of ≥5 required evidence days.
3. SSC has no corrected v1.0.1 economic baseline yet; HYP_001 is quarantined pending preregistration repair.
4. Proposal-identity reconciliation (WP7) remains incomplete (4 coexisting schemes).
5. ES-S4 exists only as foreign, uncommitted, self-reported WIP.

## 11. Next Safe Gates (derived, not executed)

- SSC: owner adjudication of HYP_001_EXIT_CAPTURE preregistration repair (no economic replay in Sprint 1/2 per mission boundary).
- External source: wait for the ES-S4 owning session to commit/freeze its work through the normal process; do not start a second comparison.
- Large-SMC: continue the friction campaign (WAIT_WINDOW) until ≥5 trading days are collected, then run the aggregation script.
- BTC: diagnose/restore Bybit market-data access (CONTINUE_OBSERVATION blocked on external data issue).

No economic replay, no ES-S4 comparison, and no protected evidence was accessed in producing this document.
